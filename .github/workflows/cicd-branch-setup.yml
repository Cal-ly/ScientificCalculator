name: cicd-setup

on:
  push:
    branches:
      - cicd-setup

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '8.0.x'

    - name: Install dependencies
      run: dotnet restore

    - name: Build the solution
      run: dotnet build --no-restore --configuration Release

    - name: Run unit tests
      run: dotnet test --no-build --verbosity normal --configuration Release
      
  deploy:
    if: github.ref == 'refs/heads/cicd-setup'
    needs: build-and-test
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '8.0.x'

    - name: Build the setup project
      run: |
        dotnet build --configuration Release
        dotnet publish --configuration Release -o ScientificCalculatorRelease/
    - name: List output directory contents (for debugging)
      run: |
        echo "Listing contents of ScientificCalculatorRelease/ directory:"
        dir ScientificCalculatorRelease/
    - name: Create GitHub Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.SCICALC_TOKEN }}
      with:
        tag_name: v1.0.${{ github.run_number }}-${{ github.sha }}
        release_name: Release ${{ github.run_number }}
        body: Automated release generated by GitHub Actions.
        draft: false
        prerelease: false

    - name: Upload Release Asset
      uses: actions/upload-release-asset@v1
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ScientificCalculatorRelease/ScientificCalculatorRelease.msi
        asset_name: ScientificCalculatorRelease.msi
        asset_content_type: application/octet-stream
